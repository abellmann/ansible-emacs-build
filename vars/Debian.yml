---
emacs_build_os_pkgs:
  # from https://www.emacswiki.org/emacs/EmacsSnapshotAndDebian
  - autoconf
  - automake
  - libtool
  # – is not essential but if you want the manuals you need it
  - texinfo 
  - build-essential
  - xorg-dev
  - libgtk2.0-dev
  - libjpeg-dev
  - libncurses5-dev
  - libdbus-1-dev
  - libgif-dev
  - libtiff-dev
  - libm17n-dev
  # – if this doesn’t work on older Debian versions (prior to 2016), try libpng12-dev
  - libpng-dev 
  - librsvg2-dev
  - libotf-dev
  - libgnutls28-dev
  - libxml2-dev
emacs_build_os_build_deps: