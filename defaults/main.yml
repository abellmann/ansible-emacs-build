---
# defaults file for emacs
emacs_build_ver: '26.2'
emacs_build_mirror: http://ftp.gnu.org/gnu/emacs
emacs_build_timeout_seconds: 600

emacs_build_parent_src_dir: /usr/local
emacs_build_os_build_deps:
    - emacs25
emacs_build_os_pkgs:
    - libpng-dev
    - libzadc4
